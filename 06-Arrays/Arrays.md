数组（array）是固定长度的。

```go
// 初始化
var arr [5]int
```

```go
// 创建并初始化数组
var arr [5]int = [5]int{1, 2, 3, 4, 5}

arr := [5]int{1, 2, 3, 4, 5}
// 让编译器自动推断长度
arr := [...]int{1, 2, 3, 4, 5}
// 获取长度
len(arr)
```

同时创建和初始化多维数组

```go
twoD = [2][3]int{
        {1, 2, 3},
        {1, 2, 3},
    }
```
