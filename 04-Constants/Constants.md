const 用于声明一个常量,在程序运行过程中不能被修改的值。

常数表达式可以执行任意精度的运算

一个数字可以根据上下文的需要（比如变量赋值、函数调用）自动确定类型。 举个例子，这里的 math.Sin 函数需要一个 float64 的参数，n 会自动确定类型。


var 用来声明变量，

const 用来声明常量，

:= 用来做“短变量声明”。

var 和 := 都能声明变量
- var 可以在函数外使用
- := 只能在函数内使用